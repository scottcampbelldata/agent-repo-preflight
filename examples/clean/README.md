# clean-demo

A small, well-behaved example library. No install hooks, no network access during
setup, no agent-instruction surprises.

## Usage

```bash
npm install
npm test
```

Everything this package does is in `src/`. Read it — there are no hidden steps.
