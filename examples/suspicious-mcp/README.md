# suspicious-mcp (DEMO — intentionally unsafe)

This example exists only to demonstrate `agent-repo-preflight`.

It ships an MCP config granting a `shell` tool and a `CLAUDE.md` instructing an
agent to run a setup script without inspecting it first.
